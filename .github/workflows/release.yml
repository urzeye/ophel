name: "📦 Build & Release Extension"

on:
  push:
    tags:
      - "v*.*.*" # 匹配 v1.0.0, v1.2.3 等格式的 tag
  workflow_dispatch:
    inputs:
      version:
        description: "版本号 (如 v1.0.0)"
        required: true
        default: "v1.0.0"

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write # 需要写权限来创建 release

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all platforms
        run: pnpm build:all

      - name: Build Userscript
        run: pnpm build:userscript

      - name: Package all platforms
        run: pnpm package:all

      - name: Get version
        id: get_version
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            echo "VERSION=${{ github.event.inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Rename artifacts for release
        run: |
          cp build/chrome-mv3-prod.zip build/ophel-${{ steps.get_version.outputs.VERSION }}-chrome.zip
          cp build/chrome-mv3-prod.zip build/ophel-${{ steps.get_version.outputs.VERSION }}-edge.zip
          cp build/firefox-mv3-prod.zip build/ophel-${{ steps.get_version.outputs.VERSION }}-firefox.zip
          cp build/userscript/ophel.user.js build/ophel-${{ steps.get_version.outputs.VERSION }}.user.js

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          name: Ophel ${{ steps.get_version.outputs.VERSION }}
          body: |
            ## Ophel ${{ steps.get_version.outputs.VERSION }}

            ### 📥 Download / 下载

            | Platform / 平台 | File / 文件 | Note / 说明 |
            |-----------------|-------------|-------------|
            | **Chrome** | `ophel-${{ steps.get_version.outputs.VERSION }}-chrome.zip` | Chrome Web Store or Load Unpacked / 开发者模式加载 |
            | **Edge** | `ophel-${{ steps.get_version.outputs.VERSION }}-edge.zip` | Edge Add-ons or Load Unpacked / 开发者模式加载 |
            | **Firefox** | `ophel-${{ steps.get_version.outputs.VERSION }}-firefox.zip` | Firefox Add-ons or Temporary Load / 临时加载 |
            | **Userscript / 油猴脚本** | `ophel-${{ steps.get_version.outputs.VERSION }}.user.js` | Requires Tampermonkey / 需要安装 Tampermonkey |

            ### 🔧 Installation / 安装方法

            #### Chrome / Edge
            1. Download and extract the zip file. / 下载并解压 zip 文件。
            2. Open `chrome://extensions/` or `edge://extensions/`. / 打开扩展管理页面。
            3. Enable "Developer mode". / 启用“开发者模式”。
            4. Click "Load unpacked" and select the extracted folder. / 点击“加载已解压的扩展程序”选择文件夹。

            #### Firefox
            1. Download the Firefox zip file. / 下载 Firefox 版本 zip 文件。
            2. Go to `about:debugging#/runtime/this-firefox`. / 打开调试页面。
            3. Click "Load Temporary Add-on" and select the zip file. / 点击“临时载入附加组件”选择 zip 文件。

            #### Userscript (Tampermonkey) / 油猴脚本
            1. Install [Tampermonkey](https://www.tampermonkey.net/). / 安装 Tampermonkey 插件。
            2. Click the `.user.js` file link or drag it into the browser. / 点击 `.user.js` 文件或将其拖入浏览器。
            3. Click "Install" in the Tampermonkey tab. / 在弹出的标签页点击“安装”。

            ---
            *Full Changelog / 完整更新日志: [CHANGELOG.md](https://github.com/urzeye/ophel/blob/main/CHANGELOG.md)*
          files: |
            build/ophel-${{ steps.get_version.outputs.VERSION }}-chrome.zip
            build/ophel-${{ steps.get_version.outputs.VERSION }}-edge.zip
            build/ophel-${{ steps.get_version.outputs.VERSION }}-firefox.zip
            build/ophel-${{ steps.get_version.outputs.VERSION }}.user.js
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') || contains(github.ref, 'rc') }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
